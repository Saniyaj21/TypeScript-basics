// types in ts

// number, string, boolean
// null, undefined, void
// objects, array, tuples
// any, never, unknown

let userName: string = "saniyaj"; //bad code
let userName2 = "saniyaj"; //good code

// TS is stmart enough to catch the type of the variable if value is assigning while declaring the variable

// good code
let h: string;
h = "hellow";
h = "name";

export {};
